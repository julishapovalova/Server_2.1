package server;

import java.net.Socket;

class SocketFromClient {

    Socket socket;
    String name;

    public SocketFromClient(Socket socket) {
        this.socket = socket;
        this.name = name;
    }
}
